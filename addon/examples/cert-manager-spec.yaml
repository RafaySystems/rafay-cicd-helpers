---
kind: AddonVersion
metadata:
  # version name
  name: v1
  # project name. optional
  project: defaultproject
spec:
  # addon name
  addon: cert-manager
  # namespace name
  namespace: cert-manager
  # template for different addon types
  template:
    # type of addon
    type: NativeYaml
    yamlFile: addon/examples/cert-manager.yaml

